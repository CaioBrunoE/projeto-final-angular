import { NumerosDirective } from './numeros.directive';

describe('NumerosDirective', () => {
  it('should create an instance', () => {
    const directive = new NumerosDirective();
    expect(directive).toBeTruthy();
  });
});
